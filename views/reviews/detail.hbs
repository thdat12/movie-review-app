<h2 style="text-align: center; color: teal">Detail Review</h2>
<div class="row">
  <div class="col s12 m8">
    <h3>{{review.title}}
      <small>{{{editIcon review.writer user review._id false}}}</small>
    </h3>
    <div class="card story">
      <div class="card-content">
        <span class="card-title">{{formatDate date "MMMM Do YYYY, h:mm:ss a"}}</span>
        {{stringTags review.body}}
      </div>
    </div>
  </div>
  <div class="col s12 m4">
    <div class="card center-align">
      <div class="card-content">
        <span class="card-title">{{review.writer.displayName}}</span>
        <img src="{{review.writer.image}}" alt="avatar" class="circle responsive-img img-small">
      </div>
      <div class="card-action" >
        <a href="/reviews/user/{{review.writer._id}}" style="text-align: center;color:teal">More from {{review.writer.displayName}}</a>
      </div>
    </div>
  </div>
</div>